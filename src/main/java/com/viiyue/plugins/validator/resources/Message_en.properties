# Common
validator.handler.test-passed = Data validation passed
validator.handler.test-rejected = Data validation is invalid
validator.handler.missing-value = Missing validation value
validator.handler.empty-elements = No element found for validation

# Required
validator.handler.required = Cannot be null
validator.handler.not-blank = Cannot be a blank string
validator.handler.not-empty = Cannot be an empty string

# Regular expression
validator.handler.pattern = Must match expression "{0}"

# List
validator.handler.contains = Can only be values in list( {elements} )

# Minimum value
validator.handler.min = Must be greater than or equal to {0}
validator.handler.min.java.lang.String = Needs at least {0} characters

# Maximum value
validator.handler.max = Must be less than or equal to {0}
validator.handler.max.java.lang.String = Supports up to {0} characters

# Range
validator.handler.range = Must be between {0} and {1}
validator.handler.range.java.lang.String = Must be between {0} and {1} characters

# Length
validator.handler.length = Only {0} {{0} > 1 ? 'elements' : 'element'} allowed
validator.handler.length.java.lang.String = Length must be equal to {0}

# Boundarys
validator.handler.prefixs = Can only start with ({elements})
validator.handler.suffixs = Can only end with ({elements})

# Common processor
validator.handler.ip = Not a valid IP address
validator.handler.qq = Can only be a set of numbers greater than or equal to 4 digits in length
validator.handler.fax = Not a valid FAX address
validator.handler.rar = Unsupported compressed file suffix (e.g. rar|zip|7zip|tgz)
validator.handler.url = Not a valid URL
validator.handler.ascii = Not a valid ASCII code
validator.handler.email = Must be a well-formed email address
validator.handler.idcard = Not a valid ID number
validator.handler.postal-code = Not a valid postal code

# Letter
validator.handler.letter = Not a valid letter combination
validator.handler.letter-lowercase = Not a valid lowercase letter combination
validator.handler.letter-uppercase = Not a valid capital letter combination

# Chinese
validator.handler.chinese = Only Chinese

# Color
validator.handler.colour = Not a valid hexadecimal color value, it should be start with "\#"
validator.handler.colour-hex = Not a valid hexadecimal color value, it should be start with "\#"
validator.handler.colour-rgb = Not a valid RGB color, it should start with "rgb"

# Account
validator.handler.username = Can only contain letters, numbers, minus signs, underlines and some special characters (e.g. $.-_)

# Password
validator.handler.password.default = Can only be numbers, letters, or special symbols (e.g. !@#$%^&*.)
validator.handler.password.medium = Must contain one or more of letters, numbers, and special numbers (e.g. !@#$%^&*.)
validator.handler.password.strong = Must contain uppercase, lowercase, numbers, and special symbols (e.g. !@#$%^&*.)

# Compared
validator.handler.equals.related = Different from {target} value
validator.handler.equals.specify = Can only be "{0}"

# Phone
validator.handler.cellphone = Not a valid mobile number
validator.handler.telephone = Not a valid landline number

# Number
validator.handler.int = Not a valid integer number
validator.handler.int-nagative = Must be an integer number less than 0
validator.handler.int-positive = Must be an integer number greater than 0

validator.handler.float = Not a valid floating number
validator.handler.float-nagative = Must be a floating number less than 0
validator.handler.float-positive = Must be a floating number greater than 0
